import 'package:flutter/material.dart';

class PastChatsTab extends StatelessWidget {
  const PastChatsTab({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(child: Text('Past Chats')),
    );
  }
}
